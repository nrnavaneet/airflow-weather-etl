# airflow-weather-etl
This project is an end-to-end ETL (Extract, Transform, Load) pipeline built using Apache Airflow (Astro CLI) and PostgreSQL, designed to fetch weather data from the Open-Meteo API, process it, and store it in a Postgres database. The pipeline is fully containerized and easy to run locally using Docker.
